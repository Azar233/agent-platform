# 商品检测故障排查

## 漏检

先检查置信度阈值是否过高、商品在画面中的尺寸、遮挡、反光、拍摄角度和训练集覆盖情况。降低阈值只能用于诊断，不能替代补充有效训练样品。

## 误检

检查相似包装商品、背景干扰、标注框是否准确以及类别样本是否不均衡。必要时加入容易混淆的负样本或补充不同环境下的场景图。

## 模型测试

测试时应记录模型版本、数据集来源、置信度、IoU、输入图片和结果。比较模型时应使用同一批测试素材和相同推理参数。

图片、视频和 ZIP 检测结果属于实时业务数据，必须通过检测工具查询，不能从知识库推断。
