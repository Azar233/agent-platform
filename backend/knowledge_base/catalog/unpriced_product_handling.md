# 未定价商品处理

商品映射存在但找不到有效价格记录时，`has_price=false`。检测结果仍可显示类别和数量，但价格汇总必须标记 `pricing_complete=false`，列出未定价类别和目标数量。

顾客结算端不得把未定价商品按 0 元、缓存价格或知识库中的示例价格结算。经营者应在管理端选择正确数据集和稳定商品，实时查询确认后发起改价预览。

未定价可能来自新商品尚未设置价格、清价操作、错误的数据集商品映射或旧 `category_id` 无法安全对应。先确认 `product_id` 和版本映射，再设置价格；不要为绕过问题创建重复商品。
