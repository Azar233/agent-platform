# 金额精度、币种与显示

当前管理表单支持 CNY 和 USD，默认 CNY。金额必须为非负数，输入步长为 0.01；显示和业务传输应保持两位小数语义，数据库与计算过程避免使用二进制浮点直接累加造成舍入误差。

同一次结算的商品价格应使用一致币种。若商品币种不一致，系统不能在没有明确汇率服务的情况下自动换算或直接相加。知识库不保存汇率和实时价格。

影响预览必须同时显示数值与币种，例如 `3.50 CNY`。清价后保留商品身份和默认显示币种提示，但 `unit_price` 为空；顾客端显示“未定价”而不是 `0.00`。
