# 聊天中后选图片覆盖先选图片

## 现象

附件选择器一次可见一张；选择第二张后第一张从待发送列表消失。

## 根因

文件 input 虽声明 multiple，但每次 change 都先清空附件状态和预览 URL，再保存本次选择。

## 解决方案

附件改为追加队列，按文件名、大小、修改时间和 MIME 去重；累计执行 30 个上限。只在用户删除、发送成功或离开页面时释放对应预览 URL。

## 验证

一次选择多张和分多次选择都能保留全部附件；删除单张不影响其他附件；上传请求包含全部文件。
